# ═══════════════════════════════════════════════════════════════
# Modulys Pax — Chat Service (exemplo)
# ═══════════════════════════════════════════════════════════════

PAX_CHAT_HOST=0.0.0.0
PAX_CHAT_PORT=9001

PAX_ADMIN_API_URL=http://localhost:3000/api/admin
# Mesmo valor da Admin API (PAX_SERVICE_KEY)
PAX_SERVICE_KEY="modulys-pax-internal-service-key-dev-change-in-production"
# Mesmo valor da Admin API (PAX_JWT_SECRET) — usado para validar token no WebSocket
PAX_JWT_SECRET="modulys-pax-jwt-secret-dev-change-in-production"

LOG_LEVEL=info
# Em produção, liste origens permitidas (ex: PAX_CORS_ORIGIN=https://app.exemplo.com)
# PAX_CORS_ORIGIN=
